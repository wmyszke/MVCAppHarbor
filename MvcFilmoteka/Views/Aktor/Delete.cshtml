@model MvcFilmoteka.Models.Aktor

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Aktor</legend>

    <div class="display-label">Imie</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Imie)
    </div>

    <div class="display-label">Nazwisko</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Nazwisko)
    </div>

    <div class="display-label">Data_urodzenia</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Data_urodzenia)
    </div>

    <div class="display-label">Miejsce_urodzenia</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Miejsce_urodzenia)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
